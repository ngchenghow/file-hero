name: file_hero
description: Portable SSD file manager with shared per-file readme metadata.
publish_to: none
version: 0.1.12+13
environment:
  sdk: '>=3.5.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
